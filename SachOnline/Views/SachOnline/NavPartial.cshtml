@model IEnumerable<SachOnline.Models.CHUDE>

<!-- Brand and toggle get grouped for better mobile display -->
<div class="navbar-header">
    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar1" aria-expanded="false">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
    </button>
    <a class="navbar-brand" href="#">SachOnline</a>
</div>

<!-- Collect the nav links, forms, and other content for toggling -->
<div class="collapse navbar-collapse" id="navbar1">
    <ul class="nav navbar-nav">
        <li class="active"><a href="@Url.Action("Index", "SachOnline")">TRANG CHỦ</a> </li>
        <li><a href="#">GIỚI THIỆU</a> </li>
        <li><a href="#">LIÊN HỆ</a> </li>
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false" aria-haspopup="true">SÁCH THEO CHỦ ĐỀ<span class="caret"></span></a>
            
            <ul class="dropdown-menu">
                @foreach (var item in Model)
                {
                    <li>@Html.ActionLink(item.TenChuDe, "SachTheoChuDe", new { id = item.MaCD }, "SachOnline")</li>

                    <li role="separator" class="divider"></li>
                }
            </ul>
        </li>
        <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false" aria-haspopup="true">SÁCH THEO NHÀ XUẤT BẢN<span class="caret"></span></a>
            <ul class="dropdown-menu">
                <li><a href="#">Kim đồng</a> </li>
                <li role="separator" class="divider"></li>
                <li><a href="#">Đại học Sư Phạm</a> </li>
                <li role="separator" class="divider"></li>
                <li><a href="#">Giáo dục</a> </li>
                <li role="separator" class="divider"></li>
                <li><a href="#">Thanh niên</a> </li>

            </ul>
        </li>
    </ul>
    <form class="navbar-form navbar-right" role="search">
        <div class="form-group">
            <input type="text" class="form-control" placeholder="Tìm kiếm">
        </div>
        <button type="submit" class="btn btn-default">Tìm</button>
    </form>

</div>
